No external API integration: Phase 1 sets up Supabase platform infrastructure (Edge Functions runtime, Postgres connection, Cron, Queues) — this is runtime/deployment plumbing, not a discrete external product API with a capability surface to decide INTEGRATE/OPT-OUT coverage on. The detector's `true` signal was a false positive matching the word "API" inside a doc-reference note about `docs/03-INTEGRACAO-TINY-ERP.md`. The actual Tiny ERP API integration (with a real capability surface: products, customers, orders, OAuth2) happens in Phase 3, where this checkpoint should fire for real.
